# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# LLM
litellm>=1.50.0

# NLP
medspacy>=1.2.0
spacy>=3.7.0

# Graph DB
neo4j>=5.25.0

# Vector DB
# Pin qdrant-client to match the server version deployed in
# docker-compose.prod.yml. Major.minor must match between client
# and server or you get a noisy UserWarning on every call; worse,
# some methods silently stop working when they drift too far.
qdrant-client>=1.12.0,<1.13.0
fastembed>=0.4.0

# Observability
# Pin Langfuse to v2.x — v3 and v4 have API incompatibilities with LiteLLM's
# built-in Langfuse integration. v2.60.x is the last stable v2 line.
langfuse>=2.60.0,<5.0.0
structlog>=24.4.0
prometheus-client>=0.21.0

# Security
slowapi>=0.1.9

# HTTP
httpx>=0.27.0

# Prompt templates
pyyaml>=6.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx
